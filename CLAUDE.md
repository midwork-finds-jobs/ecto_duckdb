# ecto wrapper for using DuckDB with elixir

**IMPORTANT:Always verify that migrations are working in the ./sample_phoenix project**

You can do this with:

```sh
cd ./sample_phoenix && mix ecto.migrate --pool-size 1
```
